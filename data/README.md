# Data Folder

Place the input dataset file below in this folder before running the notebook:

- `Buchan_Daily_Production_Data.csv`

The notebook reads from:

- `data/Buchan_Daily_Production_Data.csv`

This repository intentionally excludes raw CSV data from version control.
